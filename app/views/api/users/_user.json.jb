{
  id: @user.id,
  user_name: @user.user_name,
  email: @user.email,
  address: @user.address,
  bio: @user.bio,
  profile_image: @user.profile_image,
  posts: @user.posts.map { |post| post[:title] },
  replies: @user.replies.map { |reply| reply[:post_id] },
}
